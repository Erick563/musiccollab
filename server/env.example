# Configurações do Servidor
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# Banco de Dados
DATABASE_URL=postgresql://user:password@localhost:5432/musiccollab

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Socket.IO
# IMPORTANTE: Configure corretamente para evitar problemas de CORS
# Para desenvolvimento local: http://localhost:3000
# Para rede local: http://192.168.x.x:3000 (use o IP da sua máquina)
# Para múltiplas origens: http://localhost:3000,http://192.168.1.10:3000
SOCKET_CORS_ORIGIN=http://localhost:3000

# Configurações de Performance e Timeout (Opcionais - valores padrão já estão otimizados)
# As configurações abaixo já estão implementadas no código, não é necessário defini-las aqui
# Mas estão documentadas para referência:
# - pingTimeout: 60000ms (60s) - Tempo para aguardar resposta do ping
# - pingInterval: 25000ms (25s) - Intervalo entre pings para detectar conexões mortas
# - connectTimeout: 45000ms (45s) - Timeout para conexão inicial
# - server.timeout: 120000ms (120s) - Timeout geral do servidor HTTP
# - server.keepAliveTimeout: 65000ms (65s) - Timeout para conexões keep-alive

# Upload de Arquivos
UPLOAD_MAX_SIZE=50000000
UPLOAD_PATH=./uploads

# Logs
LOG_LEVEL=info
LOG_FILE=./logs/app.log